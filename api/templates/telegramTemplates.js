/**
 * Telegram Templates
 * Текстовые шаблоны для Telegram-уведомлений
 */

/**
 * Вспомогательная функция для склонения слова "день"
 * @param {number} days - Количество дней
 * @returns {string} Правильное склонение
 */
function getDaysWord(days) {
  const lastDigit = days % 10;
  const lastTwoDigits = days % 100;

  if (lastTwoDigits >= 11 && lastTwoDigits <= 19) {
    return 'дней';
  }

  if (lastDigit === 1) {
    return 'день';
  }

  if (lastDigit >= 2 && lastDigit <= 4) {
    return 'дня';
  }

  return 'дней';
}

/**
 * Сообщение о скором истечении подписки
 * @param {string} userName - Имя пользователя
 * @param {number} daysLeft - Количество дней до истечения
 * @param {string} expirationDate - Дата истечения подписки (DD.MM.YYYY)
 * @param {string} renewUrl - URL для продления подписки (опционально)
 * @returns {Object} Объект сообщения для Telegram Bot API
 */
function getSubscriptionExpiringMessage(userName, daysLeft, expirationDate, renewUrl = 'https://fohow.ru/subscription') {
  const urgencyEmoji = daysLeft <= 3 ? '🚨' : '⏰';
  const urgencyText = daysLeft <= 3 ? '*СРОЧНО*' : 'скоро';

  const text = `${urgencyEmoji} *Здравствуйте, ${userName}!*

Ваша подписка ${urgencyText} истекает!

📅 *Осталось:* ${daysLeft} ${getDaysWord(daysLeft)}
🗓 *Дата истечения:* ${expirationDate}

💎 *Что вы получаете с премиум-подпиской:*
✅ Неограниченный доступ ко всем материалам
✅ Приоритетная поддержка
✅ Эксклюзивный контент
✅ Без рекламы

Продлите подписку прямо сейчас, чтобы не потерять доступ к премиум-функциям! 🚀`;

  return {
    text,
    parse_mode: 'Markdown',
    reply_markup: {
      inline_keyboard: [
        [
          {
            text: '🔄 Продлить подписку',
            url: renewUrl
          }
        ],
        [
          {
            text: '💬 Связаться с поддержкой',
            url: 'https://t.me/fohow_support'
          }
        ]
      ]
    }
  };
}

/**
 * Сообщение об истечении подписки
 * @param {string} userName - Имя пользователя
 * @param {string} pricingUrl - URL страницы с тарифами (опционально)
 * @returns {Object} Объект сообщения для Telegram Bot API
 */
function getSubscriptionExpiredMessage(userName, pricingUrl = 'https://fohow.ru/pricing') {
  const text = `📋 *Здравствуйте, ${userName}!*

К сожалению, срок действия вашей премиум-подписки истёк.

🔄 *Переход на бесплатный план*
Ваш аккаунт автоматически переведён на бесплатный тариф. Вы можете продолжать пользоваться базовыми функциями сервиса.

📦 *Что доступно на бесплатном тарифе:*
• Ограниченный доступ к материалам
• Базовые функции сервиса
• Стандартная поддержка

━━━━━━━━━━━━━━━━━━━━

💡 *Хотите вернуть все возможности?*
Оформите новую подписку и снова получите полный доступ ко всем премиум-функциям, эксклюзивному контенту и приоритетной поддержке! ⭐`;

  return {
    text,
    parse_mode: 'Markdown',
    reply_markup: {
      inline_keyboard: [
        [
          {
            text: '⭐ Выбрать тариф',
            url: pricingUrl
          }
        ],
        [
          {
            text: '❓ Задать вопрос',
            url: 'https://t.me/fohow_support'
          }
        ]
      ]
    }
  };
}

/**
 * Приветственное сообщение для новых пользователей
 * @param {string} userName - Имя пользователя
 * @param {number} demoDays - Количество дней демо-периода (по умолчанию 3)
 * @param {string} dashboardUrl - URL личного кабинета (опционально)
 * @returns {Object} Объект сообщения для Telegram Bot API
 */
function getWelcomeMessage(userName, demoDays = 3, dashboardUrl = 'https://fohow.ru/dashboard') {
  const text = `🎉 *Добро пожаловать в FOHOW!*

Здравствуйте, *${userName}*!

Рады приветствовать вас в нашем сервисе! Спасибо, что присоединились к нам. 🚀

🎁 *Подарок от нас!*
Мы активировали для вас *демо-период на ${demoDays} ${getDaysWord(demoDays)}* с полным доступом ко всем премиум-функциям. Попробуйте все возможности платформы абсолютно бесплатно!

✨ *Что вас ждёт:*
💎 Полный доступ ко всем материалам и функциям
🎯 Эксклюзивный контент для премиум-пользователей
💬 Приоритетная поддержка от нашей команды
📚 Обучающие материалы и гайды
🚫 Без рекламы — только полезный контент

━━━━━━━━━━━━━━━━━━━━

📝 *С чего начать?*
1️⃣ Зайдите в личный кабинет
2️⃣ Изучите доступные материалы
3️⃣ Настройте профиль под себя
4️⃣ Наслаждайтесь всеми возможностями!

💡 *Совет:* Не забудьте изучить все разделы в течение демо-периода, чтобы понять, какие возможности вам наиболее интересны!

Если у вас возникнут вопросы или нужна помощь, мы всегда на связи! 💙`;

  return {
    text,
    parse_mode: 'Markdown',
    reply_markup: {
      inline_keyboard: [
        [
          {
            text: '🚀 Перейти в личный кабинет',
            url: dashboardUrl
          }
        ],
        [
          {
            text: '📚 Обучающие материалы',
            url: 'https://fohow.ru/tutorials'
          },
          {
            text: '💬 Поддержка',
            url: 'https://t.me/fohow_support'
          }
        ]
      ]
    }
  };
}

/**
 * Сообщение об активации новой подписки
 * @param {string} userName - Имя пользователя
 * @param {string} planName - Название тарифа (Premium, Individual)
 * @param {number} amount - Сумма оплаты
 * @param {string} currency - Валюта (RUB)
 * @param {string} expiresDate - Дата окончания (DD.MM.YYYY)
 * @param {string} boardsUrl - URL к доскам (опционально)
 * @returns {Object} Объект сообщения для Telegram Bot API
 */
function getSubscriptionActivatedMessage(userName, planName, amount, currency, expiresDate, boardsUrl = 'https://interactive.marketingfohow.ru/boards') {
  const text = `✅ *Подписка активирована!*

Здравствуйте, *${userName}*!

🎉 Спасибо за оплату подписки на FOHOW Interactive Board!

━━━━━━━━━━━━━━━━━━━━
*📋 Детали подписки:*

💎 Тариф: *${planName}*
💰 Стоимость: *${amount} ${currency}*
📅 Действует до: *${expiresDate}*

━━━━━━━━━━━━━━━━━━━━

*🚀 Ваши возможности:*
${planName === 'Premium'
  ? '✅ Безлимитное количество досок\n✅ Все премиум-функции\n✅ Приоритетная поддержка'
  : '✅ До 9 интерактивных досок\n✅ Расширенные функции\n✅ Экспорт в PNG'}

Приятной работы! 🎨`;

  return {
    text,
    parse_mode: 'Markdown',
    disable_web_page_preview: true,
    reply_markup: {
      inline_keyboard: [
        [
          {
            text: '🚀 Начать работу',
            url: boardsUrl
          }
        ]
      ]
    }
  };
}

/**
 * Сообщение о продлении подписки
 * @param {string} userName - Имя пользователя
 * @param {string} planName - Название тарифа
 * @param {number} amount - Сумма оплаты
 * @param {string} currency - Валюта
 * @param {string} expiresDate - Новая дата окончания (DD.MM.YYYY)
 * @param {string} boardsUrl - URL к доскам (опционально)
 * @returns {Object} Объект сообщения для Telegram Bot API
 */
function getSubscriptionRenewedMessage(userName, planName, amount, currency, expiresDate, boardsUrl = 'https://interactive.marketingfohow.ru/boards') {
  const text = `🔄 *Подписка продлена!*

Здравствуйте, *${userName}*!

Оплата успешно прошла, подписка автоматически продлена. ✅

━━━━━━━━━━━━━━━━━━━━
*📋 Детали продления:*

💎 Тариф: *${planName}*
💰 Сумма: *${amount} ${currency}*
📅 Действует до: *${expiresDate}*

━━━━━━━━━━━━━━━━━━━━

Спасибо за то, что остаётесь с нами! 💙`;

  return {
    text,
    parse_mode: 'Markdown',
    disable_web_page_preview: true,
    reply_markup: {
      inline_keyboard: [
        [
          {
            text: '📊 Перейти к доскам',
            url: boardsUrl
          }
        ]
      ]
    }
  };
}

/**
 * Сообщение об отмене подписки
 * @param {string} userName - Имя пользователя
 * @param {string} planName - Название отменённого тарифа
 * @param {string} expiresDate - Дата окончания доступа (DD.MM.YYYY)
 * @param {string} pricingUrl - URL страницы с тарифами (опционально)
 * @returns {Object} Объект сообщения для Telegram Bot API
 */
function getSubscriptionCancelledMessage(userName, planName, expiresDate, pricingUrl = 'https://interactive.marketingfohow.ru/pricing') {
  const text = `⚠️ *Подписка отменена*

Здравствуйте, *${userName}*!

Подписка *${planName}* была отменена. Вы переведены на гостевой тариф.

━━━━━━━━━━━━━━━━━━━━
*⏰ Доступ сохраняется до:* ${expiresDate}

После этой даты будут доступны только функции гостевого тарифа.

━━━━━━━━━━━━━━━━━━━━

💡 Вы можете продлить подписку в любой момент и снова получить полный доступ ко всем возможностям! 🚀`;

  return {
    text,
    parse_mode: 'Markdown',
    disable_web_page_preview: true,
    reply_markup: {
      inline_keyboard: [
        [
          {
            text: '🔄 Продлить подписку',
            url: pricingUrl
          }
        ]
      ]
    }
  };
}

/**
 * Уведомление об отключении Telegram-уведомлений
 *
 * @param {string} userName - Имя пользователя
 * @param {string} profileUrl - URL настроек профиля
 * @returns {Object} Объект сообщения для Telegram
 */
function getTelegramDisconnectedMessage(userName, profileUrl) {
  const text = `
🔕 *Уведомления отключены*

Здравствуйте, *${userName}*!

Вы отключили важные уведомления своего аккаунта Интерактивной доски FOHOW.

⚠️ *Теперь вы не будете получать:*
• Напоминания о сроках подписки
• Уведомления о продлении подписки
• Информацию об истечении подписки
• Важные обновления и новости

━━━━━━━━━━━━━━━━━━━━

💡 Чтобы снова включить уведомления, зайдите в [настройки профиля](${profileUrl}).

━━━━━━━━━━━━━━━━━━━━

📢 Наш канал по маркетингу: @MarketingFohow
  `.trim();

  return {
    text,
    parse_mode: 'Markdown',
    disable_web_page_preview: true,
    reply_markup: {
      inline_keyboard: [
        [
          { text: '⚙️ Настройки профиля', url: profileUrl }
        ],
        [
          { text: '📢 Канал по маркетингу', url: 'https://t.me/MarketingFohow' }
        ]
      ]
    }
  };
}

export {
  getSubscriptionExpiringMessage,
  getSubscriptionExpiredMessage,
  getWelcomeMessage,
  getSubscriptionActivatedMessage,
  getSubscriptionRenewedMessage,
  getSubscriptionCancelledMessage,
  getTelegramDisconnectedMessage
};

