/**
 * Telegram Templates
 * Текстовые шаблоны для Telegram-уведомлений
 */

/**
 * Вспомогательная функция для склонения слова "день"
 * @param {number} days - Количество дней
 * @returns {string} Правильное склонение
 */
function getDaysWord(days) {
  const lastDigit = days % 10;
  const lastTwoDigits = days % 100;

  if (lastTwoDigits >= 11 && lastTwoDigits <= 19) {
    return 'дней';
  }

  if (lastDigit === 1) {
    return 'день';
  }

  if (lastDigit >= 2 && lastDigit <= 4) {
    return 'дня';
  }

  return 'дней';
}

/**
 * Сообщение о скором истечении подписки
 * @param {string} userName - Имя пользователя
 * @param {number} daysLeft - Количество дней до истечения
 * @param {string} expirationDate - Дата истечения подписки (DD.MM.YYYY)
 * @param {string} renewUrl - URL для продления подписки (опционально)
 * @returns {Object} Объект сообщения для Telegram Bot API
 */
function getSubscriptionExpiringMessage(userName, daysLeft, expirationDate, renewUrl = 'https://interactive.marketingfohow.ru') {
  const text = `Здравствуйте, *${userName}*!

Ваша подписка скоро заканчивается!

🗓 *Осталось:* ${daysLeft} ${getDaysWord(daysLeft)}
📅 *Дата окончания:* ${expirationDate}

Продлите подписку сейчас, чтобы не потерять доступ к премиум‑функциям! 🚀`;

  return {
    text,
    parse_mode: 'Markdown',
    reply_markup: {
      inline_keyboard: [
        [
          {
            text: '🔄 Продлить подписку',
            url: renewUrl
          }
        ],
        [
          {
            text: '📢 MarketingFohow',
            url: 'https://t.me/MarketingFohow'
          }
        ]
      ]
    }
  };
}

/**
 * Сообщение об истечении подписки
 * @param {string} userName - Имя пользователя
 * @param {string} pricingUrl - URL страницы с тарифами (опционально)
 * @returns {Object} Объект сообщения для Telegram Bot API
 */
function getSubscriptionExpiredMessage(userName, pricingUrl = 'https://interactive.marketingfohow.ru') {
  const text = `🧾 Здравствуйте, *${userName}*!

К сожалению, срок действия вашей премиум‑подписки истёк.

🪪 *Переход на гостевой тариф*
Ваш аккаунт автоматически переведён на гостевой тариф.

✅ *Что сейчас доступно:*
• До 3‑х досок
• До 10 стикеров
• До 10 заметок и комментариев
• Скачать доску (структуру) как изображение (в формате A4)

⭐ *Хотите вернуть все возможности?*`;

  return {
    text,
    parse_mode: 'Markdown',
    disable_web_page_preview: true,
    reply_markup: {
      inline_keyboard: [
        [
          {
            text: '⭐ Выбрать тариф',
            url: pricingUrl
          }
        ],
        [
          {
            text: '📢 MarketingFohow',
            url: 'https://t.me/MarketingFohow'
          }
        ]
      ]
    }
  };
}


/**
 * Приветственное сообщение для новых пользователей
 * @param {string} userName - Имя пользователя
 * @param {number} demoDays - Количество дней демо-периода (по умолчанию 3)
 * @param {string} dashboardUrl - URL личного кабинета (опционально)
 * @returns {Object} Объект сообщения для Telegram Bot API
 */
function getWelcomeMessage(userName, demoDays = 14, dashboardUrl = 'https://interactive.marketingfohow.ru/boards') {
  const text = `Здравствуйте, *${userName}*!

Рады видеть вас! 🚀

Мы активировали для вас *${demoDays}‑дневный демо‑доступ* с полным набором премиум‑функций.

✨ *Что вы получаете:*

• Безлимитные доски
• Безлимитное количество лицензий на одной доске
• Безлимитное количество заметок на одной доске
• Безлимитное количество стикеров на одной доске
• Безлимитные комментарии
• Библиотеку изображений с возможностью создать свою собственную библиотеку
• Скачать доску (структуру) как изображение (в формате A4, A3)
• Поделиться доской (структурой) как веб‑страницей
• Режим рисования`;

  return {
    text,
    parse_mode: 'Markdown',
    reply_markup: {
      inline_keyboard: [
        [
          {
            text: '🚀 Перейти в личный кабинет',
            url: dashboardUrl
          }
        ],
        [
          {
            text: '📢 MarketingFohow',
            url: 'https://t.me/MarketingFohow'
          }
        ]
      ]
    }
  };
}

/**
 * Сообщение об активации новой подписки
 * @param {string} userName - Имя пользователя
 * @param {string} planName - Название тарифа (Premium, Individual, Demo, Guest)
 * @param {number} amount - Сумма оплаты
 * @param {string} currency - Валюта (RUB)
 * @param {string} expiresDate - Дата окончания (DD.MM.YYYY)
 * @param {string} boardsUrl - URL к доскам (опционально)
 * @returns {Object} Объект сообщения для Telegram Bot API
 */
function getSubscriptionActivatedMessage(userName, planName, amount, currency, expiresDate, boardsUrl = 'https://interactive.marketingfohow.ru/boards') {
  // Определяем возможности в зависимости от тарифа
  let features = '';

  switch (planName.toLowerCase()) {
    case 'премиум':
    case 'premium':
      features = `• Безлимитное количество досок
• Безлимитное количество стикеров
• Безлимитное количество заметок и комментариев
• До 100 папок
• Библиотека изображений
• Скачать доску (структуру) как изображение (в формате A4, A3)
• Поделиться доской (структурой) как веб‑страницей
• Дублирование досок
• Режим рисования`;
      break;

    case 'индивидуальный':
    case 'individual':
      features = `• До 18 интерактивных досок
• До 100 стикеров
• До 100 заметок и комментариев
• До 9 папок
• Библиотека изображений
• Скачать доску (структуру) как изображение (в формате A4, A3)
• Поделиться доской (структурой) как веб‑страницей
• Дублирование досок
• Режим рисования`;
      break;

    case 'демо':
    case 'demo':
      features = `• Безлимитное количество досок
• Безлимитное количество стикеров
• Безлимитное количество заметок и комментариев
• До 30 папок
• Библиотека изображений
• Скачать доску (структуру) как изображение (в формате A4, A3)
• Поделиться доской (структурой) как веб‑страницей
• Режим рисования`;
      break;

    case 'гостевой':
    case 'guest':
      features = `• До 3 интерактивных досок
• До 10 стикеров
• До 10 заметок и комментариев
• Скачать доску (структуру) как изображение (в формате A4)`;
      break;

    default:
      features = `• Расширенные функции
• Экспорт в PNG`;
  }

  const text = `✅ *Подписка активирована!*

Здравствуйте, *${userName}*!

🙏 Спасибо за оплату!

━━━━━━━━━━━━━━━━━━━━
🧾 *Детали подписки:*
💎 Тариф: *${planName}*
💰 Стоимость: *${amount} ${currency}*
📅 Действует до: *${expiresDate}*
━━━━━━━━━━━━━━━━━━━━

🚀 *Ваши возможности:*
${features}`;

  return {
    text,
    parse_mode: 'Markdown',
    disable_web_page_preview: true,
    reply_markup: {
      inline_keyboard: [
        [
          {
            text: '👤 В профиль',
            url: boardsUrl
          }
        ],
        [
          {
            text: '📢 MarketingFohow',
            url: 'https://t.me/MarketingFohow'
          }
        ]
      ]
    }
  };
}

/**
 * Сообщение о применении промокода
 * @param {string} userName - Имя пользователя
 * @param {string} promoCode - Код промокода
 * @param {string} planName - Название тарифа
 * @param {string} expiresDate - Дата окончания (DD.MM.YYYY)
 * @param {string} boardsUrl - URL к доскам
 * @returns {Object} Объект сообщения для Telegram Bot API
 */
function getPromoCodeAppliedMessage(userName, promoCode, planName, expiresDate, boardsUrl = 'https://interactive.marketingfohow.ru/boards') {
  const text = `✅ *Промокод ${promoCode} применён!*

Здравствуйте, *${userName}*!

━━━━━━━━━━━━━━━━━━━━
*📋 Детали промокода:*

💎 Тариф: *${planName}*
💰 Стоимость: *0 RUB (Промокод)*
📅 Действует до: *${expiresDate}*

━━━━━━━━━━━━━━━━━━━━

Приятной работы! 🎨`;

  return {
    text,
    parse_mode: 'Markdown',
    disable_web_page_preview: true,
    reply_markup: {
      inline_keyboard: [
        [
          {
            text: '👤 В профиль',
            url: boardsUrl
          }
        ],
        [
          {
            text: '📢 MarketingFohow',
            url: 'https://t.me/MarketingFohow'
          }
        ]
      ]
    }
  };
}

/**
 * Сообщение о продлении подписки
 * @param {string} userName - Имя пользователя
 * @param {string} planName - Название тарифа
 * @param {number} amount - Сумма оплаты
 * @param {string} currency - Валюта
 * @param {string} expiresDate - Новая дата окончания (DD.MM.YYYY)
 * @param {string} boardsUrl - URL к доскам (опционально)
 * @returns {Object} Объект сообщения для Telegram Bot API
 */
function getSubscriptionRenewedMessage(userName, planName, amount, currency, expiresDate, boardsUrl = 'https://interactive.marketingfohow.ru/boards') {
  // Определяем возможности в зависимости от тарифа
  let features = '';

  switch (planName.toLowerCase()) {
    case 'премиум':
    case 'premium':
      features = `• Безлимитное количество досок
• Безлимитное количество стикеров
• Безлимитное количество заметок и комментариев
• До 100 папок
• Библиотека изображений
• Скачать доску (структуру) как изображение (в формате A4, A3)
• Поделиться доской (структурой) как веб‑страницей
• Дублирование досок
• Режим рисования`;
      break;

    case 'индивидуальный':
    case 'individual':
      features = `• До 18 интерактивных досок
• До 100 стикеров
• До 100 заметок и комментариев
• До 9 папок
• Библиотека изображений
• Скачать доску (структуру) как изображение (в формате A4, A3)
• Поделиться доской (структурой) как веб‑страницей
• Дублирование досок
• Режим рисования`;
      break;

    case 'демо':
    case 'demo':
      features = `• Безлимитное количество досок
• Безлимитное количество стикеров
• Безлимитное количество заметок и комментариев
• До 30 папок
• Библиотека изображений
• Скачать доску (структуру) как изображение (в формате A4, A3)
• Поделиться доской (структурой) как веб‑страницей
• Режим рисования`;
      break;

    case 'гостевой':
    case 'guest':
      features = `• До 3 интерактивных досок
• До 10 стикеров
• До 10 заметок и комментариев
• Скачать доску (структуру) как изображение (в формате A4)`;
      break;

    default:
      features = `• Расширенные функции
• Экспорт в PNG`;
  }

  const text = `🧩 *Подписка продлена!*

Здравствуйте, *${userName}*!

Оплата успешно прошла. ✅

━━━━━━━━━━━━━━━━━━━━
💎 Тариф: *${planName}*
💰 Сумма: *${amount} ${currency}*
📅 Действует до: *${expiresDate}*
━━━━━━━━━━━━━━━━━━━━

🚀 *Ваши возможности:*
${features}`;

  return {
    text,
    parse_mode: 'Markdown',
    disable_web_page_preview: true,
    reply_markup: {
      inline_keyboard: [
        [
          {
            text: '👤 В профиль',
            url: boardsUrl
          }
        ],
        [
          {
            text: '📢 MarketingFohow',
            url: 'https://t.me/MarketingFohow'
          }
        ]
      ]
    }
  };
}

/**
 * Сообщение об одобрении верификации
 * @param {string} personalId - Компьютерный номер
 * @param {string} profileUrl - URL профиля
 * @returns {Object} Объект сообщения для Telegram Bot API
 */
function getVerificationApprovedMessage(personalId, profileUrl = 'https://interactive.marketingfohow.ru/') {
  const text = `✅ Поздравляем! Ваша заявка на верификацию одобрена!

⭐ Компьютерный номер ${personalId} успешно верифицирован.

Теперь ваш профиль отмечен значком верификации.`;

  return {
    text,
    parse_mode: 'Markdown',
    reply_markup: {
      inline_keyboard: [
        [
          {
            text: '👤 В профиль',
            url: profileUrl
          }
        ],
        [
          {
            text: '📢 MarketingFohow',
            url: 'https://t.me/MarketingFohow'
          }
        ]
      ]
    }
  };
}

/**
 * Сообщение об отмене верификации
 * @param {string} profileUrl - URL профиля
 * @returns {Object} Объект сообщения для Telegram Bot API
 */
function getVerificationRevokedMessage(profileUrl = 'https://interactive.marketingfohow.ru/') {
  const text = `⚠️ Ваш статус верификации был снят из-за изменения компьютерного номера или представительства.`;

  return {
    text,
    parse_mode: 'Markdown',
    reply_markup: {
      inline_keyboard: [
        [
          {
            text: '👤 В профиль',
            url: profileUrl
          }
        ],
        [
          {
            text: '📢 MarketingFohow',
            url: 'https://t.me/MarketingFohow'
          }
        ]
      ]
    }
  };
}

/**
 * Сообщение об автоматическом отклонении заявки (номер верифицирован другим)
 * @param {string} personalId - Компьютерный номер
 * @param {string} profileUrl - URL профиля
 * @returns {Object} Объект сообщения для Telegram Bot API
 */
function getVerificationAutoRejectedMessage(personalId, profileUrl = 'https://interactive.marketingfohow.ru/') {
  const text = `❌ Ваша заявка на верификацию отклонена

Номер ${personalId || 'не указан'} уже верифицирован другим пользователем.

Если вы считаете, что это ваш номер, обратитесь в поддержку.`;

  return {
    text,
    parse_mode: 'Markdown',
    reply_markup: {
      inline_keyboard: [
        [{ text: '📞 Поддержка', url: 'https://t.me/FOHOWadmin' }],
        [{ text: '👤 В профиль', url: profileUrl }],
        [{ text: '📢 MarketingFohow', url: 'https://t.me/MarketingFohow' }]
      ]
    }
  };
}

/**
 * Уведомление об отключении Telegram-уведомлений
 *
 * @param {string} userName - Имя пользователя
 * @param {string} profileUrl - URL настроек профиля
 * @returns {Object} Объект сообщения для Telegram
 */
function getTelegramDisconnectedMessage(userName, profileUrl) {
  const text = `
🔕 *Уведомления отключены*

Здравствуйте, *${userName}*!

Вы отключили важные уведомления своего аккаунта Интерактивной доски FOHOW.

⚠️ *Теперь вы не будете получать:*
• Напоминания о сроках подписки
• Уведомления о продлении подписки
• Информацию об истечении подписки
• Важные обновления и новости

━━━━━━━━━━━━━━━━━━━━

💡 Чтобы снова включить уведомления, зайдите в [настройки профиля](${profileUrl}).

━━━━━━━━━━━━━━━━━━━━`.trim();

  return {
    text,
    parse_mode: 'Markdown',
    disable_web_page_preview: true,
    reply_markup: {
      inline_keyboard: [
        [
          { text: '👤 В профиль', url: profileUrl }
        ],
        [
          { text: '📢 MarketingFohow', url: 'https://t.me/MarketingFohow' }
        ]
      ]
    }
  };
}

/**
 * Сообщение об отклонении заявки на верификацию (администратором)
 * @param {string} rejectionReason - Причина отклонения
 * @param {string} profileUrl - URL профиля
 * @returns {Object} Объект сообщения для Telegram Bot API
 */
function getVerificationRejectedMessage(rejectionReason, profileUrl = 'https://interactive.marketingfohow.ru/') {
  const text = `❌ Ваша заявка на верификацию отклонена

Причина: ${rejectionReason}

Вы можете подать новую заявку через 24 часа.`;

  return {
    text,
    parse_mode: 'Markdown',
    reply_markup: {
      inline_keyboard: [
        [{ text: '👤 В профиль', url: profileUrl }],
        [{ text: '📢 MarketingFohow', url: 'https://t.me/MarketingFohow' }]
      ]
    }
  };
}

export {
  getSubscriptionExpiringMessage,
  getSubscriptionExpiredMessage,
  getWelcomeMessage,
  getSubscriptionActivatedMessage,
  getPromoCodeAppliedMessage,
  getSubscriptionRenewedMessage,
  getTelegramDisconnectedMessage,
  getVerificationApprovedMessage,
  getVerificationRevokedMessage,
  getVerificationAutoRejectedMessage,
  getVerificationRejectedMessage
};
