# Руководство по проекту FOHOW Interactive Board

> **Этот раздел для тебя, Сергей.**
> Здесь всё объясняется простым языком без технических терминов.

## Что это за проект?

FOHOW Interactive Board — это веб-приложение (сайт) для создания интерактивных досок.
На этих досках пользователи строят структуры из карточек, соединяют их линиями,
добавляют картинки и заметки. Это помогает визуализировать MLM-структуры и
планировать развитие партнёрской сети.

---

## Как устроен проект (простыми словами)

Проект состоит из трёх больших частей:

### 1. Frontend (то, что видит пользователь)
📁 Папка: `src/`

Это интерфейс сайта — кнопки, панели, холст для рисования.
Когда пользователь открывает сайт, его браузер загружает эту часть.

**Аналогия:** Это как витрина магазина — то, что видят покупатели.

### 2. Backend (сервер, невидимая часть)
📁 Папка: `api/`

Это программа, которая работает на сервере. Она:
- Проверяет пароли при входе
- Сохраняет доски в базу данных
- Отправляет уведомления в Telegram
- Загружает картинки на Яндекс.Диск

**Аналогия:** Это как склад магазина — покупатели его не видят, но без него магазин не работает.

### 3. База данных (хранилище)
🗄 Расположена на отдельном сервере Beget

Здесь хранятся все данные:
- Пользователи и их пароли
- Доски и их содержимое
- Подписки и платежи
- Изображения (метаданные)

**Аналогия:** Это как картотека или архив — вся информация хранится здесь.

---

## Схема работы

```
👤 Пользователь открывает сайт
        ↓
🖥 Браузер загружает Frontend
        ↓
👤 Пользователь нажимает "Сохранить"
        ↓
🖥 Frontend отправляет запрос на Backend
        ↓
⚙️ Backend сохраняет данные в Базу данных
        ↓
✅ Backend отвечает "Успешно"
        ↓
🖥 Frontend показывает "Сохранено!"
```

---

## Главные части интерфейса

### Холст (Canvas)
📁 `src/components/Canvas/`

Это главная рабочая область — белое поле, на котором пользователь:
- Размещает карточки
- Рисует линии между ними
- Добавляет картинки и стикеры
- Двигает и масштабирует всё это

**Главный файл:** `CanvasBoard.vue` — это "мозг" холста.
Раньше он был огромным (6000 строк), теперь мы разделили его на части.

### Панели
📁 `src/components/Panels/`

Боковые панели с инструментами:
- Левая панель — список досок
- Правая панель — свойства выбранного объекта

### Профиль пользователя
📁 `src/components/UserProfile.vue`

Страница настроек пользователя:
- Личные данные
- Подписка
- Настройки аккаунта

### Админ-панель
📁 `src/components/Admin/`

Панель управления для администраторов:
- Список пользователей
- Модерация изображений
- Статистика

---

## Подробные руководства

| Тема | Файл | Описание |
|------|------|----------|
| Как работает холст | [how-canvas-works.md](how-canvas-works.md) | Подробно про рабочую область |
| Composables | [composables-simple.md](composables-simple.md) | Что это за файлы и зачем они нужны |

---

## Частые вопросы

### Что-то сломалось на сайте, куда смотреть?

1. Открой сайт
2. Нажми F12 (откроется панель разработчика)
3. Перейди на вкладку "Console"
4. Там будут красные сообщения об ошибках
5. Сделай скриншот и отправь программисту

### Как понять, какой файл отвечает за функцию?

Смотри таблицу в [how-canvas-works.md](how-canvas-works.md) — там расписано,
какой файл за что отвечает.

### Где хранятся картинки пользователей?

Картинки хранятся на Яндекс.Диске. В базе данных только информация о них
(название, размер, кто загрузил).

### Как добавить нового администратора?

Через базу данных — нужно в таблице `users` поставить `role = 'admin'`.
Это делается через SQL-запрос.
