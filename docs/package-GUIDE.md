# PACKAGE-GUIDE.md
Полное руководство по файлу **package.json**
Проект: **FOHOW Interactive Board**

---

## Оглавление

1. [Введение](#введение)
2. [Что такое packagejson](#что-такое-packagejson)
3. [Структура файла](#структура-файла)
4. [Подробный разбор каждого блока](#подробный-разбор-каждого-блока)
   - [4.1 Основная информация](#41-основная-информация)
   - [4.2 Требования к nodejs](#42-требования-к-nodejs)
   - [4.3 Скрипты](#43-скрипты)
   - [4.4 Настройки pnpm](#44-настройки-pnpm)
   - [4.5 Зависимости](#45-зависимости)
   - [4.6 Dev-зависимости](#46-dev-зависимости)
5. [Раздел для новичка: простое объяснение](#раздел-для-новичка-простое-объяснение)
6. [Иконки и визуальные подсказки](#иконки-и-визуальные-подсказки)
7. [Как использовать документ](#как-использовать-документ)
8. [Полный пакет документации проекта](#полный-пакет-документации-проекта)

---

## Введение

Этот документ создан для того, чтобы подробно и простым языком объяснить файл **package.json**, который лежит в основе проекта **FOHOW Interactive Board**.

Документ подходит:

- новичкам
- разработчикам
- тем, кто будет поддерживать проект
- будущим программистам, которым нужно быстро вникнуть

---

## Что такое packagejson

`package.json` — это конфигурационный файл фронтенд-проекта.
Он описывает:

- какие библиотеки нужны
- какие версии использовать
- какие команды можно запускать
- на какой версии Node работает проект
- как собирать и деплоить приложение

Если удалить package.json — проект не запустится.

---

## Структура файла

Файл содержит 6 основных секций:

1. Общая информация
2. Требования к Node.js
3. Скрипты
4. Настройки pnpm
5. Зависимости
6. Dev-зависимости

---

## Подробный разбор каждого блока

### 4.1 Основная информация

```json
{
  "name": "fohow-interactive-board",
  "version": "0.0.0",
  "private": true,
  "type": "module"
}
```

**Объяснение:**

| Поле | Значение |
|------|----------|
| name | название проекта |
| version | текущая версия |
| private | запрещает публикацию пакета в npm |
| type | включает современную систему модулей (ESM) |

---

### 4.2 Требования к Nodejs

```json
"engines": {
  "node": "^20.19.0 || >=22.12.0"
}
```

Проект должен работать на:

- Node 20.19+
- или Node 22.12+

Это гарантирует стабильную работу всех зависимостей и скриптов.

---

### 4.3 Скрипты

```json
"scripts": {
  "dev": "vite",
  "build": "vite build",
  "preview": "vite preview",
  "test": "node --test tests/*.spec.js",
  "deploy": "npm run build && cd dist && git init && git branch -M gh-pages && git add . && git commit -m \"Deploy to GitHub Pages\" && git remote add origin https://github.com/sergeyxmao/FOHOW-proekt-v3.git && git push -f origin gh-pages",
  "deploy:windows": "deploy.bat"
}
```

| Команда | Что делает |
|--------|------------|
| npm run dev | запускает Vite в режиме разработки |
| npm run build | собирает продакшн-бандл |
| npm run preview | показывает локальный предпросмотр сборки |
| npm test | запускает нативные node-тесты из `tests/*.spec.js` |
| npm run deploy | автоматизирует деплой на GitHub Pages через dist-папку |
| npm run deploy:windows | запускает Windows-скрипт деплоя |

---

### 4.4 Настройки pnpm

```json
"pnpm": {
  "neverBuiltDependencies": [
    "rollup"
  ]
}
```

Эта секция подсказывает менеджеру пакетов, что зависимость `rollup` не требует пересборки при установке. Это ускоряет установку и предотвращает возможные конфликты сборки.

---

### 4.5 Зависимости

Это библиотеки, которые нужны приложению в работе.

```json
"dependencies": {
  "@vueuse/core": "^13.9.0",
  "cropperjs": "^1.6.2",
  "html2canvas": "^1.4.1",
  "jspdf": "^3.0.3",
  "pinia": "^3.0.3",
  "vue": "^3.5.22",
  "vue-i18n": "^9.14.5",
  "vue-router": "^4.6.3"
}
```

| Пакет | Назначение |
|-------|------------|
| vue | основной фреймворк |
| vue-router | маршрутизация между страницами |
| pinia | глобальное состояние приложения |
| vue-i18n | поддержка многоязычности |
| @vueuse/core | набор готовых Vue-хуков |
| html2canvas | конвертация интерфейса в изображение |
| jspdf | генерация PDF-файлов |
| cropperjs | обрезка и управление изображениями |

---

### 4.6 Dev-зависимости

```json
"devDependencies": {
  "@vitejs/plugin-vue": "^6.0.1",
  "vite": "^7.1.7",
  "vite-plugin-vue-devtools": "^8.0.2"
}
```

Эти пакеты используются только при разработке.

| Пакет | Назначение |
|-------|------------|
| vite | сборщик и dev-сервер |
| @vitejs/plugin-vue | подключение Vue к Vite |
| vite-plugin-vue-devtools | дополнительные инструменты отладки |

---

## Раздел для новичка: простое объяснение

Представь, что проект — это дом.

- package.json — проектная документация
- dependencies — строительные материалы (кирпичи, окна)
- devDependencies — инструменты (дрель, отвёртка)
- scripts — пошаговые инструкции:
  - «Начать стройку» — dev
  - «Построить дом» — build
  - «Показать готовый дом» — preview
  - «Выполнить контроль качества» — test
  - «Показать дом заказчику» — deploy

Без package.json никто не поймёт, что строить и как.

---

## Иконки и визуальные подсказки

Для визуализации можно использовать:

- 📦 — для зависимостей
- 🛠 — для команд
- ⚙️ — для настроек
- 🧱 — для блоков конфигурации

---

## Как использовать документ

Этот гид помогает:

- быстро понять структуру package.json
- объяснить команду новым разработчикам
- использовать файл как шпаргалку во время настройки окружения
- прикладывать документ к README или внутренним инструкциям

---

## Полный пакет документации проекта

Дополнительно стоит подготовить:

- подробный README.md с инструкциями по запуску
- описание структуры папок и компонентов Vue
- гайд по тестированию и покрытию тестами
- регламент по деплою (включая автоматизацию для разных ОС)
- список рекомендаций по поддержанию з
