
Проект: **FOHOW Interactive Board**

---

## Оглавление

1. [Введение](#введение)
2. [Подробный разбор файла](#подробный-разбор-файла)
   - [Основные поля](#основные-поля)
   - [Требования к Node.js](#требования-к-nodejs)
   - [Скрипты npm](#скрипты-npm)
   - [Настройки pnpm](#настройки-pnpm)
   - [Рабочие зависимости](#рабочие-зависимости)
   - [Dev-зависимости](#dev-зависимости)
3. [Раздел для новичка](#раздел-для-новичка)
4. [Иконки и визуальные подсказки](#иконки-и-визуальные-подсказки)

---

## Введение

Документ описывает структуру и назначение полей файла **package.json** в проекте *FOHOW Interactive Board*. Он помогает быстро разобраться, как устроен менеджмент зависимостей, какие версии инструментов требуются и какие команды доступны для работы с приложением.

---

## Подробный разбор файла

### Основные поля

```json
{
  "name": "fohow-interactive-board",
  "version": "0.0.0",
  "private": true,
  "type": "module"
}
```

| Поле | Значение |
|------|----------|
| `name` | Указывает официальное имя проекта. |
| `version` | Фиксирует текущую версию приложения. |
| `private` | Запрещает публикацию пакета в публичном npm-реестре. |
| `type` | Переключает Node.js на систему модулей ECMAScript (ESM). |

---

### Требования к Node.js

```json
"engines": {
  "node": "^20.19.0 || >=22.12.0"
}
```

Поле гарантирует, что проект будет запускаться только на Node.js 20.19.0 и выше либо на ветке 22.12.0 и новее. Это важно для совместимости зависимостей и корректного выполнения скриптов.

---

### Скрипты npm

```json
"scripts": {
  "dev": "vite",
  "build": "vite build",
  "preview": "vite preview",
  "test": "node --test tests/*.spec.js",
  "deploy": "npm run build && cd dist && git init && git branch -M gh-pages && git add . && git commit -m \"Deploy to GitHub Pages\" && git remote add origin https://github.com/sergeyxmao/FOHOW-proekt-v3.git && git push -f origin gh-pages",
  "deploy:windows": "deploy.bat"
}
```

| Команда | Назначение |
|---------|------------|
| `npm run dev` | Запускает Vite в режиме разработки с горячей перезагрузкой. |
| `npm run build` | Собирает приложение в директорию `dist` для продакшна. |
| `npm run preview` | Локально показывает собранную версию из `dist`. |
| `npm test` | Запускает нативные Node.js тесты из папки `tests`. |
| `npm run deploy` | Собирает проект и выкладывает содержимое `dist` в ветку `gh-pages`. |
| `npm run deploy:windows` | Использует отдельный батник для деплоя из Windows-среды. |

---

### Настройки pnpm

```json
"pnpm": {
  "neverBuiltDependencies": [
    "rollup"
  ]
}
```

Настройка сообщает менеджеру пакетов, что `rollup` не нужно пересобирать после установки, что ускоряет процесс и исключает возможные ошибки компиляции.

---

### Рабочие зависимости

```json
"dependencies": {
  "@vueuse/core": "^13.9.0",
  "cropperjs": "^1.6.2",
  "html2canvas": "^1.4.1",
  "jspdf": "^3.0.3",
  "pinia": "^3.0.3",
  "vue": "^3.5.22",
  "vue-i18n": "^9.14.5",
  "vue-router": "^4.6.3"
}
```

| Пакет | Роль в проекте |
|-------|----------------|
| `vue` | Основной фронтенд-фреймворк Vue 3. |
| `vue-router` | Управление маршрутами и страницами. |
| `pinia` | Централизованное хранилище данных. |
| `vue-i18n` | Механизм перевода интерфейса на разные языки. |
| `@vueuse/core` | Набор полезных Vue-хуков и утилит. |
| `html2canvas` | Создание изображений из DOM-элементов. |
| `jspdf` | Генерация PDF-документов на клиенте. |
| `cropperjs` | Обрезка и трансформация изображений. |

---

### Dev-зависимости

```json
"devDependencies": {
  "@vitejs/plugin-vue": "^6.0.1",
  "vite": "^7.1.7",
  "vite-plugin-vue-devtools": "^8.0.2"
}
```

| Пакет | Роль в разработке |
|-------|-------------------|
| `vite` | Сборщик, dev-сервер и оптимизатор модулей. |
| `@vitejs/plugin-vue` | Интеграция Vue-компонентов в Vite. |
| `vite-plugin-vue-devtools` | Расширенные инструменты отладки для Vue-проектов. |

---

## Раздел для новичка

Представьте, что проект — это интерактивная доска. Файл `package.json` — это чек-лист со всеми инструментами и инструкциями. Скрипты подсказывают, какую кнопку нажать, зависимости описывают, что нужно установить, а dev-зависимости — какие инструменты взять разработчику. Следуя этому файлу, можно безошибочно развернуть и поддерживать проект.

---

## Иконки и визуальные подсказки

- 📦 — зависимость, которая используется в приложении.
- 🛠 — скрипт или инструмент разработчика.
- ⚙️ — настройка окружения или менеджера пакетов.
- 🧭 — подсказка для ориентации новичка в проекте.

