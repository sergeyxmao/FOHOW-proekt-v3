# Forced Logout Flow (Принудительный выход)

> Документация по механизму автосохранения и корректного выхода при принудительном завершении сессии

## Описание

При получении события `session_forced_logout` приложение выполняет:
1. Автоматическое сохранение текущей структуры (если есть несохраненные изменения)
2. Корректный выход пользователя (аналогично ручному нажатию "Выйти")
3. Переход интерфейса в публичное состояние (пустой холст + кнопки "Войти/Регистрация")

## Затронутые файлы

| Файл | Изменения |
|------|-----------|
| `src/App.vue` | Добавлена функция `handleForcedLogout()`, слушатель события |
| `src/main.js` | Инициализация глобального перехватчика fetch |
| `src/utils/apiFetch.js` | Глобальный перехватчик fetch для обработки forced_logout |
| `api/middleware/auth.js` | Проверка существования сессии в `active_sessions` |
| `api/routes/auth.js` | Исправлен `/api/logout` (извлечение signature из JWT) |

## Функция handleForcedLogout()

**Расположение:** `src/App.vue`

### Алгоритм работы

```
┌─────────────────────────────────────┐
│     Получено session_forced_logout  │
└─────────────────┬───────────────────┘
                  │
                  ▼
┌─────────────────────────────────────┐
│  authStore.isAuthenticated === true? │
└─────────────────┬───────────────────┘
                  │ нет
                  ▼
         (выход из функции)
                  │ да
                  ▼
┌─────────────────────────────────────┐
│  currentBoardId && hasUnsavedChanges?│
└─────────────────┬───────────────────┘
                  │ нет              │ да
                  │                  ▼
                  │   ┌────────────────────────────────┐
                  │   │ Сохранение с таймаутом 5 сек   │
                  │   │ PUT /boards/:id {content: ...} │
                  │   └────────────────────────────────┘
                  │                  │
                  ▼                  ▼
┌─────────────────────────────────────┐
│         authStore.logout()          │
│    boardStore.clearCurrentBoard()   │
│          stopAutoSave()             │
└─────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────┐
│      UI в публичном состоянии       │
│  (пустой холст + Войти/Регистрация) │
└─────────────────────────────────────┘
```

### Условия сохранения

Сохранение выполняется только если:
- `authStore.isAuthenticated === true`
- `boardStore.currentBoardId !== null`
- `boardStore.hasUnsavedChanges === true`

### Таймаут

- Максимальное время на сохранение: **5 секунд**
- При превышении таймаута — logout продолжается без сохранения
- При ошибке сети — logout продолжается

## Как вызвать событие

Событие `session_forced_logout` является кастомным событием window. Для его вызова:

```javascript
// Простой вызов
window.dispatchEvent(new Event('session_forced_logout'))

// С дополнительными данными (опционально)
window.dispatchEvent(new CustomEvent('session_forced_logout', {
  detail: { reason: 'session_expired' }
}))
```

### Сценарии использования

1. **Истечение сессии** — когда backend возвращает 401/403 из-за невалидного токена
2. **Завершение сессии с другого устройства** — при превышении лимита сессий
3. **Административное завершение** — принудительный выход администратором
4. **Истечение подписки** — при окончании grace-периода

### Пример интеграции (WebSocket)

```javascript
// В обработчике WebSocket сообщений
socket.onmessage = (event) => {
  const data = JSON.parse(event.data)

  if (data.type === 'session_terminated') {
    window.dispatchEvent(new Event('session_forced_logout'))
  }
}
```

### Пример интеграции (HTTP Interceptor)

```javascript
// В axios interceptor
api.interceptors.response.use(
  response => response,
  error => {
    if (error.response?.status === 401 && error.response?.data?.code === 'SESSION_TERMINATED') {
      window.dispatchEvent(new Event('session_forced_logout'))
    }
    return Promise.reject(error)
  }
)
```

## Автоматическая проверка сессии (Backend Middleware)

**Файл:** `api/middleware/auth.js`

При каждом авторизованном запросе middleware проверяет, существует ли сессия в `active_sessions`:

```javascript
// Извлекаем signature из токена
const tokenSignature = tokenParts.length === 3 ? tokenParts[2] : null

// Проверяем существование сессии
const sessionResult = await pool.query(
  'SELECT id FROM active_sessions WHERE token_signature = $1',
  [tokenSignature]
)

if (sessionResult.rows.length === 0) {
  return reply.code(401).send({
    error: 'Сессия завершена',
    reason: 'forced_logout'  // ← Этот reason перехватывается на frontend
  })
}
```

### Когда срабатывает

- При входе на другом устройстве (если превышен лимит сессий)
- При административном завершении сессии
- При истечении сессии по времени

## Глобальный перехватчик Fetch (Frontend)

**Файл:** `src/utils/apiFetch.js`

Переопределяет глобальный `window.fetch` для автоматического перехвата 401 с `reason: 'forced_logout'`:

```javascript
// Инициализация в main.js
import { initFetchInterceptor } from './utils/apiFetch'
initFetchInterceptor()

// Внутри перехватчика:
if (response.status === 401) {
  const data = await response.clone().json()
  if (data.reason === 'forced_logout') {
    window.dispatchEvent(new CustomEvent('session_forced_logout', {
      detail: { reason: 'session_terminated_by_server' }
    }))
  }
}
```

### Защита от дублирования

Перехватчик использует флаг `isForcedLogoutInProgress` для предотвращения множественных вызовов события при нескольких параллельных запросах.

## Исправление Backend /api/logout

### Проблема (до исправления)

Сессия сохранялась по `token_signature` (последняя часть JWT), но удалялась по полному `token`:

```javascript
// БЫЛ БАГ:
await pool.query(
  'DELETE FROM active_sessions WHERE token_signature = $1',
  [token]  // ← полный токен вместо signature
)
```

### Решение

```javascript
// ИСПРАВЛЕНО:
const tokenParts = token.split('.')
const tokenSignature = tokenParts.length === 3 ? tokenParts[2] : token

await pool.query(
  'DELETE FROM active_sessions WHERE token_signature = $1 RETURNING id',
  [tokenSignature]  // ← только signature
)
```

## Ручная проверка

### Тест 1: Ручной вызов события

1. Авторизуйтесь в приложении
2. Откройте или создайте структуру
3. Внесите изменения (добавьте карточку/стикер)
4. Откройте DevTools Console
5. Выполните: `window.dispatchEvent(new Event('session_forced_logout'))`
6. Ожидаемый результат:
   - В Network: `PUT /boards/:id` (если были изменения)
   - В Console: логи `🔒 Forced logout: ...`
   - UI: пустой холст + кнопки "Войти"/"Регистрация"

### Тест 2: Автоматический forced logout при входе с другого устройства

**Требование:** Тариф с лимитом сессий = 1 (например, Demo)

1. **Вкладка A:** Авторизуйтесь, откройте структуру, внесите изменения
2. **Вкладка B (инкогнито):** Авторизуйтесь под тем же пользователем
3. **Вкладка A:** Выполните любое действие (например, сохранение) или подождите автосохранение
4. Ожидаемый результат на вкладке A:
   - В Console: `[ApiFetch] Получен forced_logout от сервера`
   - В Console: `🔒 Forced logout: ...`
   - В Network: `PUT /boards/:id` (автосохранение перед выходом)
   - UI: пустой холст + кнопки "Войти"/"Регистрация"

## Связанные файлы

- `src/stores/auth.js` — `logout()` action
- `src/stores/board.js` — `clearCurrentBoard()`, `hasUnsavedChanges`
- `src/components/Layout/MobileHeader.vue` — ручной logout через меню

## Риски

1. **Возможная задержка до 5 сек** перед logout (время на сохранение)
2. **Потеря изменений** при ошибке сети или превышении таймаута

## Откат изменений

```bash
git revert <commit-hash>
```

## История изменений

- **2026-01-21**: Добавлена автоматическая проверка сессии в middleware auth.js
- **2026-01-21**: Добавлен глобальный перехватчик fetch для обработки forced_logout
- **2026-01-21**: Создан механизм forced logout с автосохранением
- **2026-01-21**: Исправлен баг в `/api/logout` (извлечение signature)
