# LOD Minimal Mode для карточек

## Описание
При масштабе ≤20% карточки переключаются в упрощённый режим отображения.

## Что показывается
- Только заголовок карточки
- Цветной фон (без белого поля)
- Сокращённый номер партнёра (RUY68240926666 → RUY68)

## Что скрывается
- Белое поле (card-body)
- Аватарка
- Все данные (PV, баланс, актив-заказы, цикл/этап)
- Кнопка закрытия ✕
- Кнопка заметки 📝
- Точки соединения

## Технические детали
- `zoomScale` передаётся через provide/inject
- `isMinimalMode` computed: true при scale ≤ 0.20
- `shortTitle` сокращает длинные номера
- `titleAutoFitStyle` динамически вычисляет размер шрифта

## Расположение файлов
- Card.vue: src/components/Canvas/Card.vue
- CanvasBoard.vue: src/components/Canvas/CanvasBoard.vue

## История изменений
- 2026-02-04: Добавлен LOD minimal режим при ≤20%
