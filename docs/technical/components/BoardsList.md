# BoardsList.vue

## Расположение
`src/components/Board/BoardsList.vue`

## Описание
Компонент страницы "Мои структуры" — отображает список досок пользователя с возможностью организации по папкам.

## Структура layout

```
┌─────────────────────────────────────────────────────────┐
│ HEADER (фиксированная шапка)                            │
│ [← Назад] [Мои структуры] [Папки: X/Y] [+Папка] [+Доска]│
├──────────────────┬──────────────────────────────────────┤
│ FOLDERS PANEL    │ BOARDS PANEL                         │
│ (300px)          │ (1fr)                                │
│                  │                                      │
│ 📁 Все доски     │ ┌──────┐ ┌──────┐ ┌──────┐          │
│ 📂 Без категории │ │ Card │ │ Card │ │ Card │          │
│ 📁 Папка 1       │ └──────┘ └──────┘ └──────┘          │
│ 📁 Папка 2       │ ┌──────┐ ┌──────┐                    │
│                  │ │ Card │ │ Card │                    │
│                  │ └──────┘ └──────┘                    │
└──────────────────┴──────────────────────────────────────┘
```

## Ключевые CSS-стили

```css
.boards-page {
  display: flex;
  flex-direction: column;
  height: 100vh;
}

.boards-content {
  display: grid;
  grid-template-columns: 300px 1fr;  /* Левая панель 300px, правая - остаток */
  flex: 1;
  overflow: hidden;
}

.boards-list {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
  gap: 20px;
}
```

## Зависимости

### Stores (Pinia)
- `useAuthStore` — токен авторизации
- `useUserStore` — информация о тарифе пользователя
- `useBoardFoldersStore` — папки для досок
- `useNotificationsStore` — уведомления

### Компоненты
- `FeatureGate` — ограничение функций по тарифу

## Основные функции

| Функция | Описание |
|---------|----------|
| `loadBoards()` | Загрузка списка досок с API |
| `createNewBoard()` | Создание новой структуры |
| `openBoard(board)` | Открытие доски (с проверкой блокировки) |
| `deleteBoard(id)` | Удаление доски |
| `duplicateBoard(id)` | Дублирование доски |
| `renameBoard(board)` | Переименование доски |

### Функции для папок

| Функция | Описание |
|---------|----------|
| `createFolder()` | Создание папки |
| `renameFolder()` | Переименование папки |
| `deleteFolderConfirm()` | Удаление папки |
| `toggleBoardInFolder()` | Добавление/удаление доски из папки |

## Состояния отображения

1. **Loading** — спиннер загрузки
2. **Error** — сообщение об ошибке с кнопкой "Повторить"
3. **Empty** — нет досок, предложение создать первую
4. **Content** — двухпанельный layout с папками и досками

## Модальные окна

- Создание папки
- Переименование папки
- Удаление папки (с предупреждением)
- Управление папками доски
- Заблокированная доска (предложение продлить тариф)

## Drag & Drop

Поддерживается перетаскивание досок между папками:
- `handleDragStart` / `handleDragEnd` — начало/конец перетаскивания доски
- `handleDropOnFolder` — сброс доски на папку
- `handleDragEnterFolder` / `handleDragLeaveFolder` — визуальная индикация

## Важные замечания

### Статический импорт в router
Компонент импортируется статически в `src/router/index.js` для корректного применения CSS при первом рендеринге.

```js
// router/index.js
import BoardsList from '../components/Board/BoardsList.vue'
// НЕ: component: () => import('../components/Board/BoardsList.vue')
```

### Блокировка досок
Доски могут быть заблокированы при истечении подписки. Заблокированные доски:
- Отображаются с opacity 0.6 и grayscale
- При клике показывают модальное окно с предложением продлить тариф
- Удаляются автоматически через 14 дней после блокировки

## История изменений

| Дата | Изменение |
|------|-----------|
| 2026-01-05 | Удалён дублирующий блок `<style>` (был workaround для динамического импорта) |
| 2026-01-05 | Исправлена проблема с layout при первом открытии страницы |
