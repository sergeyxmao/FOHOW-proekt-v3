# ViewMenu Component

Компонент меню настроек внешнего вида приложения.

## Расположение
`src/components/Layout/ViewMenu.vue`

## Описание
ViewMenu предоставляет интерфейс для настройки визуальных параметров канваса, линий, анимации и других элементов интерфейса.

## Структура меню

### 1. Сетка (Grid)
Настройки отображения сетки на канвасе.

**Параметры**:
- **Шаг сетки**: Расстояние между линиями сетки (5-200px)
- **Фон сетки**: Переключатель видимости фона сетки

### 2. Линии (Lines)
Настройки линий соединений между карточками и аватарами.

**Параметры**:
- **Выбор цвета**: Color picker для выбора цвета линий
- **Ко всем линиям**: Checkbox для применения изменений ко всем линиям (обычным и avatar)
- **Avatar линии** 🆕: Checkbox для применения изменений только к avatar-линиям
- **Толщина**: Slider для выбора толщины линии (1-20px)

**Логика работы**:
1. Если есть выбранные линии → изменения применяются только к ним
2. Иначе если включен "Avatar линии" → изменения применяются только к avatar-линиям
3. Иначе если включен "Ко всем линиям" → изменения применяются ко всем линиям
4. Иначе → обновляются только default значения (для новых линий)

**Взаимоисключающие режимы**:
- "Ко всем линиям" и "Avatar линии" не могут быть включены одновременно
- При включении одного, другой автоматически отключается

### 3. Анимация (Animation)
Настройки анимации линий.

**Параметры**:
- **Вкл/Выкл**: Переключатель анимации
- **Цвет анимации**: Color picker для выбора цвета анимации
- **Длительность**: Поле ввода для установки длительности в секундах (2-999)

### 4. Фон (Background)
Настройки цвета фона канваса.

**Параметры**:
- **Светлый фон**: Кнопка быстрого выбора светлого фона (#f5f7fb)
- **Темный фон**: Кнопка быстрого выбора темного фона (#111827)
- **Выбор цвета**: Color picker для произвольного цвета

### 5. Цвет заголовка (Header Color)
Настройки цвета заголовков выбранных карточек.

**Параметры**:
- **Выбор цвета**: Color picker для выбора цвета
- **Сменить цвет**: Кнопка циклического переключения цветов из палитры
- **Текущий индекс**: Отображение текущего индекса цвета в палитре

### 6. Язык (Language)
Выбор языка интерфейса.

**Доступные языки**:
- 🇷🇺 Русский (ru)
- 🇬🇧 English (en)
- 🇨🇳 中文 (zh)

## Props

### isModernTheme
- **Тип**: `Boolean`
- **Default**: `false`
- **Описание**: Флаг для включения современной темной темы оформления

## Используемые Stores

### ViewSettingsStore
Управляет всеми настройками внешнего вида:
- `lineColor` - цвет линий
- `lineThickness` - толщина линий
- `isGlobalLineMode` - режим "Ко всем линиям"
- `isAvatarLineMode` 🆕 - режим "Avatar линии"
- `animationColor` - цвет анимации
- `isAnimationEnabled` - включена ли анимация
- `animationSeconds` - длительность анимации
- `headerColor` - цвет заголовка
- `backgroundGradient` - цвет фона

### CanvasStore
Управляет настройками канваса:
- `isGridBackgroundVisible` - видимость фона сетки
- `gridStep` - шаг сетки

## Методы

### toggleSubmenu(id)
Открывает/закрывает подменю по ID.

**Параметры**:
- `id` (string): ID подменю ('grid', 'lines', 'animation', 'background', 'header', 'language')

### toggleGridBackground()
Переключает видимость фона сетки.

### handleLineThickness(value)
Обрабатывает изменение толщины линии.

**Параметры**:
- `value` (number): Новая толщина (1-20)

### handleLineColorChange(event)
Обрабатывает изменение цвета линии.

**Параметры**:
- `event` (Event): Event объект от color picker

### toggleGlobalLineMode()
Переключает режим "Ко всем линиям".

### toggleAvatarLineMode() 🆕
Переключает режим "Avatar линии".

### handleAnimationChange(value)
Обрабатывает изменение длительности анимации.

**Параметры**:
- `value` (number): Новая длительность в секундах

### handleAnimationColorChange(event)
Обрабатывает изменение цвета анимации.

**Параметры**:
- `event` (Event): Event объект от color picker

### toggleAnimation()
Переключает включение/выключение анимации.

### handleHeaderColorChange(event)
Обрабатывает изменение цвета заголовка.

**Параметры**:
- `event` (Event): Event объект от color picker

### cycleHeaderColor()
Переключает цвет заголовка на следующий в палитре.

### handleBackgroundChange(event)
Обрабатывает изменение цвета фона.

**Параметры**:
- `event` (Event): Event объект от color picker

### selectPresetBackground(color)
Устанавливает предустановленный цвет фона.

**Параметры**:
- `color` (string): Цвет фона

### changeLocale(newLocale)
Изменяет язык интерфейса.

**Параметры**:
- `newLocale` (string): Код языка ('ru', 'en', 'zh')

## Стилизация

Компонент использует два варианта стилей:
1. **Светлая тема** (default) - для светлого режима
2. **Темная тема** (`view-menu--modern`) - активируется через prop `isModernTheme`

### Основные CSS классы
- `.view-menu` - контейнер меню
- `.view-menu__title` - заголовок меню
- `.view-menu__item` - элемент меню
- `.view-menu__main` - кнопка открытия подменю
- `.view-menu__submenu` - подменю с настройками
- `.view-menu__control` - кнопка управления
- `.view-menu__control--active` - активная кнопка
- `.view-menu__control--avatar` 🆕 - кнопка управления avatar-линиями
- `.view-menu__slider` - слайдер
- `.view-menu__swatch` - образец цвета

## Интернационализация

Все текстовые значения используют vue-i18n с ключами из `viewMenu.*`:
- `viewMenu.title` - заголовок меню
- `viewMenu.lines` - название секции линий
- `viewMenu.applyToAllLines` - текст для режима "Ко всем линиям"
- `viewMenu.applyToAvatarLines` 🆕 - текст для режима "Avatar линии"
- `viewMenu.lineThickness` - текст для толщины
- и т.д.

## Примеры использования

### Базовое использование
```vue
<ViewMenu :isModernTheme="false" />
```

### С темной темой
```vue
<ViewMenu :isModernTheme="true" />
```

## Взаимодействие с другими компонентами

### Canvas
ViewMenu управляет визуальными параметрами Canvas через CanvasStore:
- Фон канваса
- Видимость и шаг сетки

### Connections
ViewMenu управляет параметрами линий через ConnectionsStore (опосредованно через ViewSettingsStore):
- Цвет и толщина всех линий
- Цвет и толщина только avatar-линий 🆕
- Цвет и толщина выбранных линий
- Настройки анимации

### Cards
ViewMenu управляет цветом заголовков карточек через CardsStore (опосредованно через ViewSettingsStore).

## История изменений

### 2026-01-10: Добавлена поддержка управления avatar-линиями
- Добавлен checkbox "Avatar линии" в секцию "Линии"
- Добавлен ref `isAvatarLineMode` из ViewSettingsStore
- Добавлена функция `toggleAvatarLineMode()`
- Checkbox "Avatar линии" и "Ко всем линиям" взаимоисключающие
- Позволяет отдельно управлять настройками avatar-линий

## См. также

- [ViewSettings Store](../../stores/viewSettings.md)
- [Connections Store](../../stores/connections.md)
- [Canvas Store](../../stores/canvas.md)
