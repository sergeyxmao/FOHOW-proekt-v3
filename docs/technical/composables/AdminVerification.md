# AdminVerification.vue

## Назначение

`AdminVerification.vue` — компонент админ‑панели, который позволяет модераторам:

- Просматривать заявки на верификацию и архив решений.
- Одобрять или отклонять заявки с причиной.
- Экспортировать пользователей по статусу верификации и тарифным планам.
- Копировать реферальную ссылку пользователя из карточки заявки.

Файл: `src/components/Admin/AdminVerification.vue`.

## Основные блоки интерфейса

- **Табы**: очередь (`pending`) и архив (`archive`).
- **Карточка заявки**:
  - Персональные данные пользователя (ФИО, email, логин, компьютерный номер).
  - Реферальная ссылка с кнопкой копирования (если доступна).
  - Кнопки действий: «Одобрить» и «Отклонить».
- **Модалка отклонения**: ввод причины отклонения.

## Ключевая логика

- `loadVerifications()` — загрузка очереди заявок.
- `loadArchive()` — загрузка архива заявок.
- `handleApprove()` / `handleReject()` — действия модератора.
- `copyToClipboard()` — копирование реферальной ссылки с уведомлением через `notificationsStore`.
- Экспортные действия через `exportVerified`, `exportNonVerified`, `exportByPlan`.

## Состояния и хранилища

- **Pinia stores**:
  - `useAdminStore()` — загрузка заявок и действия модерации.
  - `useAuthStore()` — токен для запросов админ‑API.
  - `useNotificationsStore()` — показ уведомлений.
- **Локальные состояния**:
  - `processingId` — индикатор обработки заявки.
  - `activeTab`, `archiveVerifications`, `archiveLoading`, `archiveError` — архив.
  - `showExportMenu`, `subscriptionPlans`, `loadingPlans` — меню экспорта.

## API зависимости

- `GET /admin/verifications/archive` — архив заявок.
- `GET /admin/subscription-plans` — планы для экспорта.
- Экспортные эндпоинты в `exportVerified`, `exportNonVerified`, `exportByPlan`.

## Поведение копирования

Кнопка 📋 вызывает `copyToClipboard(referral_link)`:

- При успехе показывается уведомление «Ссылка скопирована…».
- При ошибке показывается уведомление об ошибке копирования.
