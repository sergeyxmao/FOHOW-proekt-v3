# Help-центр (справочная панель)

## Описание

Встроенная справочная система с CMS-функциональностью. Контент хранится в PostgreSQL и загружается через API. Администратор может редактировать статьи прямо в панели помощи через WYSIWYG-редактор на базе `contenteditable`, вставлять изображения и управлять категориями/статьями. Обычные пользователи видят панель в режиме «только чтение».

## Расположение файлов

| Файл | Описание |
|------|----------|
| `src/components/Help/HelpButton.vue` | Плавающая кнопка-триггер «?» / «✕» |
| `src/components/Help/HelpPanel.vue` | Боковая панель: API-загрузка, редактирование, WYSIWYG, управление |
| `src/components/Help/HelpImageLightbox.vue` | Полноэкранный просмотр изображений |
| `api/routes/help.js` | API эндпоинты Help-центра (10 маршрутов) |
| `api/services/yandexDiskService.js` | Функции `getHelpFolderPath()`, `getHelpImagePath()` |
| `api/scripts/migrate-help-images.js` | Одноразовый скрипт миграции с локальной ФС на Yandex.Disk |
| `src/App.vue` | Интеграция: импорт, ref `isHelpOpen`, размещение компонентов |

## Архитектура

### Кнопка (HelpButton)

- Позиция: `fixed`, left: 24px, bottom: 24px, z-index: 1800
- На мобильных: left: 8px, bottom: 72px (выше MobileToolbar)
- При открытой панели отображает «✕», в закрытом состоянии — «?»
- Скрывается: `isPencilMode`, `showResetPassword`, неавторизованные
- Класс `no-print`, поддержка светлой/тёмной темы

### Панель (HelpPanel)

- Позиция: `fixed`, right: 0, top: 0, z-index: 2100
- Ширина: 380px (desktop), 100vw (mobile)
- Overlay за панелью (закрытие кликом)
- `<Teleport to="body">`

### Три уровня навигации

1. **Список категорий** — главный экран
2. **Список статей** — при клике на категорию
3. **Содержимое статьи** — HTML-контент + изображения

### Lightbox (HelpImageLightbox)

- `position: fixed; inset: 0; z-index: 10000`
- Изображение по центру: `max-width: 90vw; max-height: 90vh`
- Закрытие: клик по overlay, кнопка ✕, Escape

## API эндпоинты

Swagger-тег: **Help** (10 эндпоинтов)

| Метод | URL | Auth | Описание |
|-------|-----|------|----------|
| GET | `/api/help/categories` | user | Все категории со статьями |
| POST | `/api/help/categories` | admin | Создать категорию |
| PUT | `/api/help/categories/:id` | admin | Обновить категорию |
| DELETE | `/api/help/categories/:id` | admin | Удалить категорию (каскадно) |
| POST | `/api/help/articles` | admin | Создать статью |
| PUT | `/api/help/articles/:id` | admin | Обновить статью (title, content) |
| DELETE | `/api/help/articles/:id` | admin | Удалить статью + изображения |
| POST | `/api/help/articles/:id/images` | admin | Загрузить изображение (5MB max) |
| GET | `/api/help/images/:filename` | public | Раздача изображения |
| DELETE | `/api/help/images/:filename` | admin | Удалить изображение |

### Формат ответа GET /api/help/categories

```json
{
  "categories": [
    {
      "id": 1,
      "icon": "🚀",
      "title": "Начало работы",
      "sort_order": 1,
      "articles": [
        {
          "id": 1,
          "title": "Регистрация и вход",
          "content": "<p>HTML контент...</p>",
          "sort_order": 1
        }
      ]
    }
  ]
}
```

## Структура БД

### help_categories

| Поле | Тип | Описание |
|------|-----|----------|
| id | SERIAL PK | |
| icon | VARCHAR | Эмодзи-иконка |
| title | VARCHAR | Название категории |
| sort_order | INTEGER | Порядок сортировки |
| created_at | TIMESTAMP | |
| updated_at | TIMESTAMP | |

### help_articles

| Поле | Тип | Описание |
|------|-----|----------|
| id | SERIAL PK | |
| category_id | INTEGER FK → help_categories | ON DELETE CASCADE |
| title | VARCHAR | Название статьи |
| content | TEXT | HTML-контент (из contenteditable) |
| sort_order | INTEGER | Порядок внутри категории |
| created_at | TIMESTAMP | |
| updated_at | TIMESTAMP | |

## Режим редактирования (только admin)

### Вход в режим

- Кнопка ✏️ в заголовке панели (видна только admin: `authStore.user.role === 'admin'`)
- При клике — `isEditMode = true`, кнопка меняется на «✓ Готово»
- При выходе — данные перезагружаются с сервера

### Редактирование текста статьи

- Контент рендерится в `<div contenteditable="true">`
- Тулбар: выбор шрифта, размер, жирный (Ж), курсив (К), вставка изображения (🖼), сохранить (💾)
- Кнопки Ж/К подсвечиваются при активном форматировании (курсор внутри bold/italic текста)
- Форматирование через `document.execCommand()` (native, без библиотек)
- Кнопки Ж/К выполняют команду на `@mousedown.prevent` (не `@click`) для надёжного сохранения выделения
- Перед каждой командой принудительно восстанавливается фокус и выделение через `restoreSelection()`
- Выделение непрерывно сохраняется через `document.addEventListener('selectionchange')`
- Автосохранение при `@blur` contenteditable

### Вставка изображений

**Способ 1: Ctrl+V** — paste-событие перехватывается, изображение загружается на сервер

**Способ 2: Кнопка 🖼** — открывает `<input type="file">`, после выбора — загрузка + вставка `<img>`

Изображения загружаются на Yandex.Disk в `{YANDEX_DISK_BASE_DIR}/HELP/` с именами: `article-{id}-{timestamp}-{random}.{ext}`

### Управление категориями

- Кнопка «+ Добавить категорию» внизу списка
- Inline-форма: emoji-picker (10 вариантов) + название + кнопка «Создать»
- Кнопка 🗑 на каждой категории (с подтверждением)

### Управление статьями

- Кнопка «+ Добавить статью» внизу списка
- Inline-форма: название + кнопка «Создать»
- Кнопка 🗑 на каждой статье (с подтверждением)

## Хранение изображений

- На **Yandex.Disk** в папке `{YANDEX_DISK_BASE_DIR}/HELP/`
- Формат имени: `article-{articleId}-{timestamp}-{random4chars}.{ext}`
- Допустимые MIME: image/jpeg, image/png, image/webp, image/gif
- Максимум: 5MB
- Раздача через `/api/help/images/:filename` — бэкенд проксирует файл с Yandex.Disk
- Download URL кэшируются в памяти (Map, TTL 1 час) для снижения нагрузки на Yandex API
- При ошибке загрузки (протухшая ссылка) — кэш сбрасывается и делается retry
- При удалении файла — запись удаляется из кэша
- При удалении статьи — все её изображения удаляются автоматически с Yandex.Disk
- При удалении категории — все изображения всех её статей удаляются

### Миграция с локальной ФС

Скрипт `api/scripts/migrate-help-images.js` переносит существующие изображения из `api/uploads/help/` на Yandex.Disk.

```bash
cd api
node scripts/migrate-help-images.js
```

- Идемпотентный — можно запускать повторно (пропускает существующие)
- НЕ удаляет локальные файлы (безопасный откат)
- Логирует прогресс и итоговую статистику
- Требует `.env` с `YANDEX_DISK_TOKEN` и `YANDEX_DISK_BASE_DIR`

## Стилизация

### Светлая тема
- Фон панели: `rgba(255, 255, 255, 0.97)` с `backdrop-filter: blur(12px)`
- Фон кнопки: `rgba(255, 255, 255, 0.92)`

### Тёмная тема (--modern)
- Фон панели: `rgba(28, 38, 58, 0.95)`
- Фон кнопки: `rgba(32, 44, 68, 0.9)`
- Текст: `#e5f3ff`

### z-index слои
- Кнопка: 1800
- Overlay: 2099
- Панель: 2100
- Lightbox: 10000

## Интеграция в App.vue

- Импорт `HelpButton` и `HelpPanel`
- Ref: `const isHelpOpen = ref(false)`
- `v-if="isAuthenticated"` — только для авторизованных
- `v-show="!isPencilMode && !showResetPassword"` — скрытие в спец. режимах

## Как добавить категорию/статью

### Через интерфейс (рекомендуется)

1. Войдите как admin
2. Откройте Help-центр (кнопка «?»)
3. Нажмите ✏️ для входа в режим редактирования
4. Нажмите «+ Добавить категорию» или «+ Добавить статью»
5. Заполните форму и нажмите «Создать»

### Через API

```bash
# Создать категорию
curl -X POST /api/help/categories \
  -H "Authorization: Bearer {token}" \
  -H "Content-Type: application/json" \
  -d '{"title": "Новая категория", "icon": "📌"}'

# Создать статью
curl -X POST /api/help/articles \
  -H "Authorization: Bearer {token}" \
  -H "Content-Type: application/json" \
  -d '{"category_id": 1, "title": "Новая статья"}'

# Обновить контент статьи
curl -X PUT /api/help/articles/1 \
  -H "Authorization: Bearer {token}" \
  -H "Content-Type: application/json" \
  -d '{"content": "<p>HTML контент...</p>"}'
```

## История изменений

| Дата | Описание |
|------|----------|
| 2026-02-20 | v1: Создание Help-центра: кнопка, панель, статические данные из helpData.js |
| 2026-02-20 | v2: CMS с API, редактирование через contenteditable, загрузка изображений, управление категориями/статьями, lightbox |
| 2026-02-20 | v2.1: Исправлено применение bold/italic, добавлена подсветка активных кнопок Ж/К |
| 2026-02-20 | v2.2: Исправлена работа кнопки Жирный: команда выполняется на mousedown (вместо click), принудительное восстановление фокуса и выделения, замена `<strong>`/`<em>` на `<span>` в кнопках тулбара |
| 2026-02-20 | v3: Миграция хранения изображений с локальной ФС (`api/uploads/help/`) на Yandex.Disk (`{BASE_DIR}/HELP/`). Добавлен in-memory кэш download URL (TTL 1 час). Создан скрипт миграции `migrate-help-images.js`. |
