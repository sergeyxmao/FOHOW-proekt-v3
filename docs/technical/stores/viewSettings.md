# ViewSettings Store

Store для управления настройками внешнего вида приложения.

## State

### lineColor
- **Тип**: `string`
- **Default**: Из профиля пользователя или `'#0f62fe'`
- **Описание**: Цвет линий соединений

### lineThickness
- **Тип**: `number`
- **Default**: Из профиля пользователя или `5`
- **Описание**: Толщина линий соединений (1-20)

### isGlobalLineMode
- **Тип**: `boolean`
- **Default**: `false`
- **Описание**: Режим "Ко всем линиям" - применять изменения ко всем линиям (обычным и avatar)

### isAvatarLineMode 🆕
- **Тип**: `boolean`
- **Default**: `false`
- **Описание**: Режим "Avatar линии" - применять изменения только к avatar-линиям

### animationColor
- **Тип**: `string`
- **Default**: Из профиля пользователя или `'#5D8BF4'`
- **Описание**: Цвет анимации линий

### isAnimationEnabled
- **Тип**: `boolean`
- **Default**: Из профиля пользователя или `true`
- **Описание**: Включена ли анимация линий

### animationSeconds
- **Тип**: `number`
- **Default**: `2`
- **Описание**: Длительность анимации в секундах (2-999)

### headerColor
- **Тип**: `string`
- **Default**: `'#5D8BF4'`
- **Описание**: Цвет заголовка карточек

### headerColorIndex
- **Тип**: `number`
- **Default**: `0`
- **Описание**: Индекс цвета заголовка в палитре

### backgroundGradient
- **Тип**: `string`
- **Default**: Из профиля пользователя или `'#b9c4da'`
- **Описание**: Цвет фона канваса

## Getters

### sliderTrackStyle
Возвращает стиль для отображения трека слайдера толщины линии.

### animationDurationMs
Преобразует animationSeconds в миллисекунды.

## Actions

### setLineColor(color)
Устанавливает цвет линий и применяет изменения согласно текущему режиму.

**Логика применения**:
1. Если есть выбранные линии (`selectedConnectionIds`) → применить только к ним
2. Иначе если включен `isAvatarLineMode` → применить только к avatar-линиям
3. Иначе если включен `isGlobalLineMode` → применить ко всем линиям
4. Иначе → обновить только default значение (для новых линий)

**Параметры**:
- `color` (string): Новый цвет линии

### setLineThickness(value)
Устанавливает толщину линий и применяет изменения согласно текущему режиму.

**Логика применения**: Аналогично `setLineColor`

**Параметры**:
- `value` (number): Новая толщина линии (1-20)

### toggleGlobalLineMode()
Переключает режим "Ко всем линиям".

**Важно**: При включении автоматически отключает `isAvatarLineMode`

### toggleAvatarLineMode() 🆕
Переключает режим "Avatar линии".

**Важно**: При включении автоматически отключает `isGlobalLineMode`

### setAnimationSeconds(value)
Устанавливает длительность анимации и применяет изменения согласно текущему режиму.

**Логика применения**: Аналогично `setLineColor`

**Параметры**:
- `value` (number): Длительность в секундах (2-999)

### setAnimationColor(color)
Устанавливает цвет анимации.

**Параметры**:
- `color` (string): Новый цвет анимации

### toggleAnimation(forceValue)
Включает/выключает анимацию линий.

**Параметры**:
- `forceValue` (boolean, optional): Принудительно установить значение

### setHeaderColor(color)
Устанавливает цвет заголовка выбранных карточек.

**Параметры**:
- `color` (string): Новый цвет заголовка

### cycleHeaderColor()
Переключает цвет заголовка на следующий в палитре.

### setBackground(gradient)
Устанавливает цвет фона канваса.

**Параметры**:
- `gradient` (string): Новый цвет фона

## Режимы работы с линиями

### Режим 1: Выбранные линии (приоритетный)
Когда пользователь выбрал одну или несколько линий:
- Изменения применяются **только к выбранным линиям**
- Работает с обычными и avatar-линиями одновременно
- Независимо от состояния `isGlobalLineMode` и `isAvatarLineMode`

### Режим 2: Avatar линии
Когда включен `isAvatarLineMode`:
- Изменения применяются **только к avatar-линиям**
- Обычные линии не затрагиваются
- Автоматически отключает `isGlobalLineMode`

### Режим 3: Все линии
Когда включен `isGlobalLineMode`:
- Изменения применяются **ко всем линиям** (обычным и avatar)
- Автоматически отключает `isAvatarLineMode`

### Режим 4: Default (по умолчанию)
Когда нет выбранных линий и оба режима выключены:
- Обновляются только **default значения**
- Применяется к новым создаваемым линиям
- Существующие линии не изменяются

## Интеграция с ConnectionsStore

ViewSettings использует следующие методы ConnectionsStore:

```javascript
// Для выбранных линий
connectionsStore.updateSelectedConnections(ids, updates)

// Для avatar-линий
connectionsStore.updateAllAvatarConnectionsColor(color)
connectionsStore.updateAllAvatarConnectionsThickness(thickness)
connectionsStore.updateAllAvatarConnections(updates)

// Для всех линий
connectionsStore.updateAllConnectionsColorIncludingAvatars(color)
connectionsStore.updateAllConnectionsThicknessIncludingAvatars(thickness)
connectionsStore.updateAllConnectionsIncludingAvatars(updates)
```

## История изменений

### 2026-01-10: Добавлена поддержка управления avatar-линиями
- Добавлен state `isAvatarLineMode`
- Добавлен action `toggleAvatarLineMode()`
- Обновлена логика в `setLineColor`, `setLineThickness`, `setAnimationSeconds`:
  - Добавлен приоритет для avatar-линий между выбранными и глобальным режимом
  - Режимы `isGlobalLineMode` и `isAvatarLineMode` взаимоисключающие
- Позволяет отдельно управлять настройками avatar-линий через ViewMenu

## См. также

- [Connections Store](./connections.md)
- [ViewMenu Component](../components/Layout/ViewMenu.md)
