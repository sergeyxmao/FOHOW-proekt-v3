```markdown
# Telegram Templates — Шаблоны Telegram-уведомлений

## Расположение

`api/templates/telegramTemplates.js`

## Назначение

Текстовые шаблоны для формирования Telegram-уведомлений с единым стилем и структурой.

## Функции

### Уведомления о подписках

#### `getSubscriptionActivatedMessage(userName, planName, amount, currency, expiresDate, boardsUrl)`

Шаблон для уведомления об активации новой подписки.

**Параметры:**
- `userName` (string) — Имя пользователя
- `planName` (string) — Название тарифа (Premium, Individual, Demo, Guest)

**Особенности:**
- Динамически подставляет возможности тарифа:
  - **Premium**: безлимит досок, стикеров, экспорт PNG (A4-A1), HTML
  - **Individual**: до 9 досок, 100 стикеров, экспорт PNG (A4-A1), HTML
  - **Demo**: до 9 досок, 15 стикеров, экспорт PNG (A4)
  - **Guest**: до 3 досок, 10 стикеров, экспорт PNG (A4), HTML

- `amount` (number) — Сумма оплаты
- `currency` (string) — Валюта (RUB)
- `expiresDate` (string) — Дата окончания в формате "DD Month YYYY" (например, "14 января 2026 г.")
- `boardsUrl` (string) — URL к доскам (по умолчанию: `https://interactive.marketingfohow.ru/boards`)

**Возвращает:**
```javascript
{
  text: string,           // Markdown-текст сообщения
  parse_mode: 'Markdown',
  disable_web_page_preview: true,
  reply_markup: {         // Кнопка "Начать работу"
    inline_keyboard: [[...]]
  }
}
```

**Пример использования:**
```javascript
import { getSubscriptionActivatedMessage } from '../templates/telegramTemplates.js';
import { sendTelegramMessage } from '../utils/telegramService.js';

const message = getSubscriptionActivatedMessage(
  'Иван Иванов',
  'Premium',
  399,
  'RUB',
  '14 февраля 2026 г.',
  'https://interactive.marketingfohow.ru/boards'
);

await sendTelegramMessage(chatId, message.text, {
  parse_mode: message.parse_mode,
  reply_markup: message.reply_markup,
  disable_web_page_preview: message.disable_web_page_preview
});
```

---

#### `getPromoCodeAppliedMessage(userName, promoCode, planName, expiresDate, boardsUrl)`

Шаблон для уведомления о применении промокода.

**Параметры:**
- `userName` (string) — Имя пользователя
- `promoCode` (string) — Код применённого промокода
- `planName` (string) — Название тарифа (Premium, Individual, Demo, Guest)
- `expiresDate` (string) — Дата окончания в формате "DD Month YYYY" (например, "14 января 2026 г.")
- `boardsUrl` (string) — URL к доскам (по умолчанию: `https://interactive.marketingfohow.ru/boards`)

**Возвращает:**
```javascript
{
  text: string,           // Markdown-текст сообщения
  parse_mode: 'Markdown',
  disable_web_page_preview: true,
  reply_markup: {         // Кнопка "Начать работу"
    inline_keyboard: [[...]]
  }
}
```

**Особенности:**
- Заголовок: "✅ Промокод ${promoCode} применён!"
- Стоимость всегда 0 RUB (Промокод)
- Без блока возможностей тарифа (более лаконичный)
- Кнопка: "🚀 Начать работу"

**Пример использования:**
```javascript
import { getPromoCodeAppliedMessage } from '../templates/telegramTemplates.js';
import { sendTelegramMessage } from '../utils/telegramService.js';

const message = getPromoCodeAppliedMessage(
  'Иван Иванов',
  'GUEST_DOWNGRADE',
  'Гостевой',
  '14 февраля 2026 г.',
  'https://interactive.marketingfohow.ru/boards'
);

await sendTelegramMessage(chatId, message.text, {
  parse_mode: message.parse_mode,
  reply_markup: message.reply_markup,
  disable_web_page_preview: message.disable_web_page_preview
});
```

---

#### `getSubscriptionRenewedMessage(userName, planName, amount, currency, expiresDate, boardsUrl)`

Шаблон для уведомления о продлении подписки.

**Параметры:** аналогично `getSubscriptionActivatedMessage()`

**Возвращает:** аналогичную структуру объекта

**Особенности:**
- Более лаконичный текст (без списка возможностей)
- Кнопка: "📊 Перейти к доскам"

---

#### `getSubscriptionCancelledMessage(userName, planName, expiresDate, pricingUrl)`

Шаблон для уведомления об отмене подписки.

**Параметры:**
- `userName` (string) — Имя пользователя
- `planName` (string) — Название отменённого тарифа
- `expiresDate` (string) — Дата окончания доступа в формате "DD.MM.YYYY"
- `pricingUrl` (string) — URL страницы с тарифами (по умолчанию: `https://interactive.marketingfohow.ru`)

**Особенности:**
- Предупреждение о блокировке досок через 14 дней
- Кнопка: "🚀 Подключить тариф"

---

### Уведомления о верификации

#### `getVerificationApprovedMessage(personalId, profileUrl)`

Сообщение об одобрении заявки на верификацию.

**Параметры:**
- `personalId` (string) — Верифицированный компьютерный номер
- `profileUrl` (string) — URL профиля (по умолчанию: `https://interactive.marketingfohow.ru/`)

**Возвращает:** объект сообщения с кнопкой "В профиль"

---

#### `getVerificationRevokedMessage(profileUrl)`

Сообщение об отмене верификации (при смене номера/представительства).

**Параметры:**
- `profileUrl` (string) — URL профиля

---

#### `getVerificationAutoRejectedMessage(personalId, profileUrl)`

Сообщение об автоматическом отклонении заявки (номер верифицирован другим пользователем).

**Параметры:**
- `personalId` (string) — Компьютерный номер, который уже верифицирован
- `profileUrl` (string) — URL профиля (по умолчанию: `https://interactive.marketingfohow.ru/`)

**Возвращает:**
```javascript
{
  text: string,           // Текст сообщения
  parse_mode: 'Markdown',
  reply_markup: {
    inline_keyboard: [
      [{ text: '📞 Написать в поддержку', url: 'https://t.me/FOHOWadmin' }],
      [{ text: '👤 В профиль', url: profileUrl }]
    ]
  }
}
```

**Особенности:**
- Две кнопки: "Написать в поддержку" и "В профиль"
- Отправляется при автоотклонении дублей заявок

---

### Прочие уведомления

#### `getSubscriptionExpiringMessage(userName, daysLeft, expirationDate, renewUrl)`

Уведомление о скором истечении подписки (отправляется автоматически за 3-7 дней).

**Параметры:**
- `userName` (string)
- `daysLeft` (number) — Количество дней до истечения
- `expirationDate` (string) — Дата истечения
- `renewUrl` (string) — URL для продления (по умолчанию: `https://interactive.marketingfohow.ru`)

**Особенности:**
- Динамический эмодзи: 🚨 если ≤3 дней, ⏰ если >3 дней
- Склонение слова "день" (день/дня/дней)
- Кнопка: "🔄 Продлить подписку" с ссылкой на главную страницу

---

#### `getSubscriptionExpiredMessage(userName, pricingUrl)`

Уведомление об истечении подписки (переход на гостевой тариф).

**Параметры:**
- `userName` (string)
- `pricingUrl` (string) — URL страницы с тарифами (по умолчанию: `https://interactive.marketingfohow.ru`)

**Особенности:**
- Показывает возможности гостевого тарифа
- Кнопка: "⭐ Выбрать тариф"

---

#### `getWelcomeMessage(userName, demoDays, dashboardUrl)`

Приветственное сообщение для новых пользователей с демо-периодом.

**Параметры:**
- `userName` (string)
- `demoDays` (number) — Количество дней демо-периода (по умолчанию: 3)
- `dashboardUrl` (string) — URL личного кабинета

---

## Стиль сообщений

### Форматирование
- **Формат:** Markdown (`parse_mode: 'Markdown'`)
- **Заголовки:** Жирный текст (`*текст*`)
- **Эмодзи:** Для визуального выделения (✅, 🔄, ⚠️, 💎, 📅, 🚀)
- **Разделители:** `━━━━━━━━━━━━━━━━━━━━` для разбивки блоков

### Структура
1. **Заголовок** с эмодзи
2. **Приветствие** с именем пользователя
3. **Основной блок** с деталями
4. **Призыв к действию** (опционально)

### Кнопки
- Всегда одна строка с 1-2 кнопками
- Первая кнопка — основное действие
- Вторая кнопка — вспомогательное (например, "Поддержка")

### Длина
- Умеренная, не более 15 строк текста
- Приветственное сообщение может быть длиннее (до 25 строк)

---

## Вспомогательные функции

### `getDaysWord(days)`

Склоняет слово "день" в зависимости от числа.

**Параметры:**
- `days` (number) — Количество дней

**Возвращает:** `"день"` | `"дня"` | `"дней"`

**Примеры:**
- `getDaysWord(1)` → `"день"`
- `getDaysWord(2)` → `"дня"`
- `getDaysWord(5)` → `"дней"`
- `getDaysWord(11)` → `"дней"` (исключение для 11-19)

---

## Интеграция с другими сервисами

### tributeService.js

Шаблоны используются в `api/services/tributeService.js` для отправки уведомлений о подписках:

```javascript
import {
  getSubscriptionActivatedMessage,
  getSubscriptionRenewedMessage,
  getSubscriptionCancelledMessage
} from '../templates/telegramTemplates.js';
```

См. подробности в `docs/technical/services/tributeService.md`.

---

## Обновление шаблонов

При изменении шаблонов:
1. ✅ Сохраняй единый стиль и структуру
2. ✅ Обновляй документацию (этот файл)
3. ✅ Тестируй отправку в реальный Telegram
4. ✅ Проверь корректность Markdown-разметки
5. ✅ Проверь работу кнопок (правильные URL)
```

***
