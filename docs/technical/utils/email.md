# Email Service — Утилиты для отправки писем

## Расположение

`api/utils/email.js`

## Зависимости

- `nodemailer` — библиотека для отправки email
- `dotenv` — для загрузки переменных окружения

## Конфигурация (env)

| Переменная | Описание |
|------------|----------|
| `EMAIL_HOST` | SMTP сервер (smtp.beget.com) |
| `EMAIL_PORT` | Порт (465 для SSL) |
| `EMAIL_USER` | Логин SMTP |
| `EMAIL_PASSWORD` | Пароль SMTP |
| `EMAIL_FROM` | Адрес отправителя |
| `FRONTEND_URL` | URL фронтенда для ссылок в письмах |

## Функции

### `sendVerificationEmail(email, code)`

Отправка кода подтверждения при регистрации.

**Параметры:**
- `email` (string) — Email получателя
- `code` (string) — 6-значный код подтверждения

**Тема письма:** "Код подтверждения — FOHOW"

---

### `sendPasswordResetEmail(email, token)`

Отправка ссылки для сброса пароля.

**Параметры:**
- `email` (string) — Email получателя
- `token` (string) — Токен для сброса пароля

**Тема письма:** "Сброс пароля — FOHOW Interactive Board"

---

### `sendWelcomeEmail(email)`

Приветственное письмо после успешной регистрации.

**Параметры:**
- `email` (string) — Email получателя

**Тема письма:** "Добро пожаловать в FOHOW"

---

### `sendSubscriptionEmail(email, eventType, data)`

Отправка email-уведомлений о событиях подписки через Tribute.

**Параметры:**
- `email` (string) — Email получателя
- `eventType` (string) — Тип события: `'new'`, `'renewed'`, `'cancelled'`
- `data` (Object) — Данные подписки:
  - `userName` (string) — Имя пользователя
  - `planName` (string) — Название тарифа
  - `amount` (number) — Сумма оплаты
  - `currency` (string) — Валюта (обычно 'RUB')
  - `startDate` (string) — Дата начала (ISO)
  - `expiresDate` (string) — Дата окончания (ISO)

**Возвращает:** `Promise<void>`

**Выбрасывает:** `Error` — при ошибке отправки

**Пример использования:**
```javascript
import { sendSubscriptionEmail } from '../utils/email.js';

await sendSubscriptionEmail('user@example.com', 'new', {
  userName: 'Иван Иванов',
  planName: 'Premium',
  amount: 399,
  currency: 'RUB',
  startDate: new Date().toISOString(),
  expiresDate: new Date(Date.now() + 30 * 24 * 60 * 60 * 1000).toISOString()
});
```

**Шаблоны писем:**

1. **'new'** — Новая подписка
   - Тема: "✅ Подписка активирована — FOHOW Interactive Board"
   - Содержание: поздравление, детали тарифа, кнопка "Начать работу"

2. **'renewed'** — Продление
   - Тема: "🔄 Подписка продлена — FOHOW Interactive Board"
   - Содержание: информация о продлении, новая дата окончания

3. **'cancelled'** — Отмена
   - Тема: "⚠️ Подписка отменена — FOHOW Interactive Board"
   - Содержание: уведомление об отмене, предложение продлить

**Стиль писем:**
- Адаптивный дизайн (max-width: 600px)
- Поддержка тёмной темы (`prefers-color-scheme: dark`)
- Цветовая схема FOHOW (зелёный #4CAF50)
- Кнопки с округлыми углами

## Обработка ошибок

Все функции логируют результат отправки:
- `✅ Email sent:` — успешная отправка
- `❌ Email error:` — ошибка отправки

При ошибке большинство функций выбрасывают исключение, кроме `sendWelcomeEmail()`, которая молча логирует ошибку.
