# VITE.CONFIG.JS-GUIDE.md
Полное руководство по файлу **vite.config.js**
Проект: **FOHOW Interactive Board**

---

## Оглавление

1. [Введение](#введение)
2. [Подробный разбор файла](#подробный-разбор-файла)
   - [Импорты](#импорты)
   - [Базовая конфигурация](#базовая-конфигурация)
   - [Подключение плагинов](#подключение-плагинов)
   - [Резолв и алиасы](#резолв-и-алиасы)
   - [Глобальные константы](#глобальные-константы)
   - [Сервер и прокси](#сервер-и-прокси)
3. [Раздел для новичка](#раздел-для-новичка)
4. [Иконки и визуальные подсказки](#иконки-и-визуальные-подсказки)

---

## Введение

Этот документ объясняет каждую строку файла **vite.config.js**, который настраивает сборку и дев-сервер Vite для проекта *FOHOW Interactive Board*.

---

## Подробный разбор файла

### Импорты

```js
import { fileURLToPath, URL } from 'node:url'
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import vueDevTools from 'vite-plugin-vue-devtools'
```

- `fileURLToPath` и `URL` — помогают корректно строить пути для алиасов.
- `defineConfig` — удобный помощник для типизации и подсветки при экспорте конфигурации Vite.
- `vue` — плагин для поддержки Vue 3 в Vite.
- `vueDevTools` — подключает инструменты разработчика Vue Devtools.

---

### Базовая конфигурация

```js
export default defineConfig({
  base: '/',
  plugins: [
    ...
  ],
  ...
})
```

- Экспорт по умолчанию: объект конфигурации, обёрнутый в `defineConfig`.
- `base: '/'` — публичный базовый путь для всех ресурсов (корень сайта).

---

### Подключение плагинов

```js
plugins: [
  vue(),
  vueDevTools({
    // Принудительно включить devtools для всех режимов (включая production)
    enabled: true,
  }),
],
```

- `vue()` — активирует поддержку `.vue` файлов, обработку SFC и компиляцию шаблонов.
- `vueDevTools({ enabled: true })` — принудительно включает Vue Devtools даже в production-сборке.
- Комментарий напоминает, что Devtools работают во всех режимах.

---

### Резолв и алиасы

```js
resolve: {
  alias: {
    '@': fileURLToPath(new URL('./src', import.meta.url))
  },
},
```

- `resolve.alias` — настраивает короткие пути.
- Алиас `@` указывает на каталог `src`, что упрощает импорты: `@/components/...` вместо относительных путей.
- `fileURLToPath(new URL('./src', import.meta.url))` безопасно преобразует URL в системный путь.

---

### Глобальные константы

```js
// Глобальные переменные для включения Vue Devtools в production
define: {
  '__VUE_PROD_DEVTOOLS__': true,
  '__VUE_OPTIONS_API__': true,
  '__VUE_PROD_HYDRATION_MISMATCH_DETAILS__': false,
},
```

- Блок `define` подставляет глобальные константы на этапе сборки.
- `__VUE_PROD_DEVTOOLS__`: `true` — разрешает Devtools в production.
- `__VUE_OPTIONS_API__`: `true` — включает поддержку Options API.
- `__VUE_PROD_HYDRATION_MISMATCH_DETAILS__`: `false` — скрывает детальные предупреждения о гидратации.

---

### Сервер и прокси

```js
// --- ДОБАВЛЕННЫЙ БЛОК ДЛЯ ПРОКСИРОВАНИЯ API-ЗАПРОСОВ ---
// Эта настройка решает проблему CORS в режиме разработки.
// Все запросы из вашего Vue-приложения, которые начинаются с "/api",
// будут автоматически перенаправлены на ваш бэкенд-сервер,
// запущенный на порту 4000.
server: {
  proxy: {
    '/api': {
      target: 'http://127.0.0.1:4000', // Адрес вашего бэкенд-сервера
      changeOrigin: true, // Необходимо для корректной работы прокси
    }
  }
}
```

- `server.proxy` — проксирование API-запросов в дев-режиме.
- Путь `/api` перенаправляется на `http://127.0.0.1:4000`.
- `changeOrigin: true` — подменяет Origin-заголовок для корректного ответа сервера.
- Комментарии фиксируют назначение блока и детали CORS.

---

## Раздел для новичка

- Vite — быстрый сборщик, этот файл говорит ему, как запускать и собирать проект.
- Плагины `vue` и `vueDevTools` нужны для работы с Vue и отладки.
- Алиас `@` позволяет писать короткие пути к файлам из `src`.
- Глобальные константы включают нужные возможности Vue даже в production.
- Прокси `/api` перенаправляет запросы на локальный бэкенд на порту 4000, чтобы избежать CORS.

---

## Иконки и визуальные подсказки

- ✅ `vue()` — активирует поддержку Vue.
- 🛠️ `vueDevTools` — всегда включённые инструменты разработки.
- 📁 `@` — алиас к каталогу `src`.
- 🌐 `server.proxy` — перенаправление запросов `/api` на локальный сервер.
- ℹ️ Рекомендация: если бэкенд меняет порт или домен, обновите `target` в блоке `proxy` и синхронизируйте его с переменными окружения.
