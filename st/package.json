{
  "name": "fohow-proekt",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "lint": "node --check auth.js && node --check card-features.js && node --check engine.js && node --check script.js",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.43.0"
  }
}
